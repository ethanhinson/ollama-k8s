apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ollama.fullname" . }}-test-connection"
  labels:
    {{- include "ollama.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ollama.fullname" . }}:11434']
  restartPolicy: Never
